public class PageRank {
    public static void main(String... args) {
        //CLIModulaire cli = new CLIModulaire();
        IHMcli ihm = new IHMcli();
        /*cli.addOption(new Option("-A", "Test", cli.config, new CommandeSetAlpha(cli.config)));
        Configuration configuration = cli.configuration(args);
        System.out.println(configuration);
        /*
        * cli.add(option(handle, description, new Commande()))
        * cli.add(option(handle, description, new Commande()))
        * */
        // Le reste du programme... Omis.
    }
}
